[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ksquare-document-extraction"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "azure-ai-documentintelligence>=1.0.0",
  "azure-identity>=1.15.0",
  "pydantic>=2.0.0",
]
