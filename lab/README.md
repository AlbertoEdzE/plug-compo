# Lab — Progressive Integration Testbed

This directory contains the progressive integration testbed ("canvas") used to validate emergent correctness across components.

Entry point:

- `python lab/run_canvas.py --phase {1,2,3,4,5,6,full}`

Reports:

- Reports are written under `lab/reports/` (gitignored).
